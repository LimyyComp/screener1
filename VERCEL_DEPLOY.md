# Деплой на Vercel

## Способ 1: Через веб-интерфейс Vercel (Самый простой)

### Шаг 1: Подготовка репозитория
1. Создайте аккаунт на GitHub (если еще нет)
2. Создайте новый репозиторий на GitHub
3. Загрузите код в репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
   git push -u origin main
   ```

### Шаг 2: Деплой на Vercel
1. Перейдите на https://vercel.com
2. Войдите через GitHub аккаунт
3. Нажмите "Add New Project"
4. Выберите ваш репозиторий
5. Vercel автоматически определит настройки:
   - **Framework Preset**: Create React App
   - **Build Command**: `npm run build`
   - **Output Directory**: `build`
6. Нажмите "Deploy"
7. Готово! Ваше приложение будет доступно по адресу типа `https://ваш-проект.vercel.app`

---

## Способ 2: Через Vercel CLI (Командная строка)

### Шаг 1: Установка Vercel CLI
```bash
npm install -g vercel
```

### Шаг 2: Вход в Vercel
```bash
vercel login
```

### Шаг 3: Деплой
```bash
vercel
```

Следуйте инструкциям:
- Set up and deploy? **Y**
- Which scope? Выберите ваш аккаунт
- Link to existing project? **N** (для первого деплоя)
- What's your project's name? Введите название
- In which directory is your code located? **./** (просто нажмите Enter)

### Шаг 4: Продакшн деплой
```bash
vercel --prod
```

---

## Способ 3: Через GitHub (Автоматический деплой)

1. Подключите репозиторий к Vercel (как в Способе 1)
2. Vercel автоматически будет деплоить при каждом push в main ветку
3. Каждый pull request получит preview URL

---

## Настройки проекта на Vercel

После первого деплоя вы можете настроить:
- **Environment Variables** (если нужны)
- **Custom Domain** (ваш домен)
- **Build Settings** (уже настроены в vercel.json)

---

## Важные моменты

✅ **Все уже настроено!** Файл `vercel.json` содержит правильную конфигурацию

✅ **WebSocket работает** - Vercel поддерживает WebSocket соединения

✅ **SPA Routing** - Настроены rewrites для правильной работы React Router (если добавите)

✅ **Автоматические деплои** - При каждом push в GitHub

---

## Быстрый старт (1 минута)

```bash
# 1. Установите Vercel CLI
npm install -g vercel

# 2. Войдите
vercel login

# 3. Деплой
vercel --prod
```

Готово! 🚀

---

## Проверка после деплоя

После деплоя проверьте:
1. ✅ Приложение открывается
2. ✅ Данные загружаются от Binance
3. ✅ WebSocket соединение работает
4. ✅ Графики отображаются
5. ✅ Фильтры и сортировка работают

---

## Полезные команды

```bash
# Посмотреть логи
vercel logs

# Посмотреть информацию о проекте
vercel inspect

# Удалить деплой
vercel remove
```

